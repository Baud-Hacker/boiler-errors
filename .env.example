# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Model Selection
# Options: gpt-4o-mini (cheap), gpt-4o (balanced), gpt-4-turbo (most capable)
MODEL=gpt-4o-mini

# Rate Limiting
# Delay between API calls in seconds
RATE_LIMIT_DELAY=1.0

# Batch Size
# Save progress after this many entries
BATCH_SIZE=10
